using System;

public enum ControlState
{
    NONE,
    PRESSED,
    HELD,
    RELEASED
}

public enum PlayerControlAction
{
    Action1
}

public enum PlatformActorState
{
    NONE = 0,
    GROUNDED = 1
}